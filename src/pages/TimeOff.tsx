import { RequestsPanel } from '@/components/requests/RequestsPanel'

/** Self-service time-off page — shared by the student and teacher portals. */
export function TimeOff() {
  return <RequestsPanel />
}
